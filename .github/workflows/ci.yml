# This is a basic workflow to help you get started with Actions

name: CI/CD Pipeline

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "master" branch
  push:
    branches: [ "master", "dev" ]
  pull_request:
    branches: [ "master" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
#      - name: Set up JDK 17
#        uses: actions/setup-java@v4
#        with:
#          java-version: 17
#          distribution: 'zulu' # Alternative distribution options are available.

      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      - name: Build and run the services with docker
#        working-directory: ./docker # Specify the target directory
        run: docker compose -f ./docker/docker-compose.testdb.yaml up --build -d

#      - name: Wait for services to be healthy (optional but recommended)
#        # Use a third-party action or a simple wait/healthcheck command
#        uses: jakejarvis/wait-action@master
#        with:
#          time: '10s' # Adjust based on your service startup time

      # Runs a single command using the runners shell
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore dependencies
        run: dotnet restore

      - name: Install SonarScanner
        run: dotnet tool install --global dotnet-sonarscanner

      - name: Build
        run: |
          export PATH="$PATH:/root/.dotnet/tools"
          dotnet-sonarscanner begin /k:"Borman2000_ASPNETCore" /d:sonar.token="sqp_e5568e5d11eca09f34ea308ce1d3f8cc95bc4318" /d:sonar.host.url="http://localhost:9000" /d:sonar.cs.opencover.reportsPaths="./IntegrationTests/coverage.xml,./UnitTests/coverage.xml" /d:sonar.coverage.exclusions="**Tests*.cs"
          dotnet build --no-restore



#          dotnet-sonarscanner begin /o:"borman2000" /k:"Borman2000_ASPNETCore" /d:sonar.token="${{ secrets.SONAR_TOKEN }}" /d:sonar.branch.name=dev
#          dotnet build --no-restore
#          dotnet-sonarscanner end /d:sonar.token="${{ secrets.SONAR_TOKEN }}"

#      - name: Cache SonarQube Cloud packages
#        uses: actions/cache@v4
#        with:
#          path: ~/sonar/cache
#          key: ${{ runner.os }}-sonar
#          restore-keys: ${{ runner.os }}-sonar
#      - name: Cache SonarQube Cloud scanner
#        id: cache-sonar-scanner
#        uses: actions/cache@v4
#        with:
#          path: ${{ runner.temp }}/scanner
#          key: ${{ runner.os }}-sonar-scanner
#          restore-keys: ${{ runner.os }}-sonar-scanner
#      - name: Install SonarQube Cloud scanner
#        if: steps.cache-sonar-scanner.outputs.cache-hit != 'true'
#        shell: bash
#        run: |
#          mkdir -p "${{ runner.temp }}/scanner"
#          dotnet tool update dotnet-sonarscanner --tool-path ${{ runner.temp }}/scanner
#      - name: Build and analyze
#        continue-on-error: true
#        env:
#          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
#        shell: bash
#        run: |
#          ${{ runner.temp }}/scanner/dotnet-sonarscanner begin -k:"Borman2000_ASPNETCore" -o:"borman2000" -d:sonar.token="${{ secrets.SONAR_TOKEN }}"
#          dotnet build --no-restore
#          ${{ runner.temp }}/scanner/dotnet-sonarscanner end -d:sonar.token="${{ secrets.SONAR_TOKEN }}"

      - name: Test
        run: dotnet test --no-build --verbosity normal /p:CollectCoverage=true /p:CoverletOutputFormat=opencover /p:CoverletOutput=./coverage.xml

      - name: SonarCube end
#          dotnet-sonarscanner end /d:sonar.token="${{ secrets.SONAR_TOKEN }}"
        run: |
          export PATH="$PATH:/root/.dotnet/tools"
          dotnet-sonarscanner end /d:sonar.token="sqp_e5568e5d11eca09f34ea308ce1d3f8cc95bc4318"

      - name: Publish
        run: dotnet publish -c Release -o ./publish
